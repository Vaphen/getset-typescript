'use babel';

import GetsetTypescriptView from '../lib/getset-typescript-view';

describe('GetsetTypescriptView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
